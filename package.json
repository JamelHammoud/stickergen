{
  "name": "stickergen",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "bot": "yarn playwright install --with-deps chromium && tsc && dotenv -e .env xvfb-run node build/bot.js",
    "commands": "tsc && dotenv -e .env node build/commands.js"
  },
  "dependencies": {
    "@napi-rs/canvas": "^0.1.44",
    "cors": "^2.8.5",
    "discord.js": "^14.13.0",
    "express": "^4.18.2",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.38.1",
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.15",
    "@types/node": "^12.0.0",
    "dotenv-cli": "^5.1.0"
  }
}
